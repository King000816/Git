plugins {
    id("com.android.application")
    id("kotlin-android")
    id("dev.flutter.flutter-gradle-plugin") // Flutter Gradle 플러그인
    id("com.google.gms.google-services") // Google 서비스 플러그인
}

android {
    namespace = "com.example.aa" // 애플리케이션의 namespace 설정
    compileSdk = 35 // SDK 버전을 35로 변경

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8 // Java 소스 호환성
        targetCompatibility = JavaVersion.VERSION_1_8 // Java 타겟 호환성
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8 // Kotlin JVM 타겟 설정
    }

    defaultConfig {
        applicationId = "com.example.aa" // 애플리케이션 ID
        minSdk = 23 // 최소 SDK 버전 (필요에 따라 조정 가능)
        targetSdk = 35 // 타겟 SDK 버전을 35로 변경
        versionCode = 1 // 버전 코드
        versionName = "1.0" // 버전 이름
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug // 릴리즈 빌드 시 디버그 서명 사용
        }
    }
}

dependencies {
    implementation(platform("com.google.firebase:firebase-bom:33.5.1")) // Firebase BoM 추가
    implementation("com.google.firebase:firebase-analytics") // Firebase Analytics 추가
    implementation("com.google.firebase:firebase-auth") // Firebase Authentication 추가
    // 필요한 다른 Firebase 의존성을 추가하세요.
}

flutter {
    source = "../.." // Flutter 소스 경로 설정
}
